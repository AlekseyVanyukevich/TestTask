@model AlertModel

@if (Model != null)
{
    <div class="alert alert-@Model.Type.ToString().ToLower() d-flex justify-content-between align-items-center">
                @Model.Content
                <button class="btn btn-outline-@Model.Type.ToString().ToLower() alert-close"></button>
    </div>
}